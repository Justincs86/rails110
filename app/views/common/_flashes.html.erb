<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert-dismissabel alert-<%= flash_class(key)%> ">
      <button class="close" data-dismiss="alert" type="button" > X </button>
      <%= value %>
    </div>
  <% end %>
<% end %>
